public Boolean ascendingOrder(Integer a, Integer b, Integer c) {
    //code here
    if ( a == null || b == null || c == null ) {
        // null pointer handle
    }

    if ( b >= a && b <= c ) {
        return true;
    } else {
        return false;
    }
}