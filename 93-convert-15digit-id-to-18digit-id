public String convert15to18DigitId(String fifteenDigit)
{
    //code here
    if ( Stirng.isBlank(fifteenDigit) || fifteenDigit.length() < 15 ) {
        return '-1';
    }
    return (String) Id.valueOf(fifteenDigit);
}